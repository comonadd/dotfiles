path=$1

# Check if file exists
if [ ! -f $path ]; then
    echo "There is no such file: $path"
    exit 1;
fi

extension="${path##*.}"
pathWithoutExtension="${path%.*}"
mkdir $pathWithoutExtension
mv $path $pathWithoutExtension/index.$extension
