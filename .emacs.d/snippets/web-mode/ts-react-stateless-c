# -*- mode: web-mode -*-
# name: ts-react-stateless-c
# key: ts-react-stateless-c
# --

type $1Props = {
    $2
}

const $1 = (props: $1Props): React.SFC<$1Props> =>
    <div className=$3>
        $4
    </div>;
