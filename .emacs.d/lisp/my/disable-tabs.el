;;; my/disable-tabs --- Package that disables the tabs in the indentation at all
;;; Commentary:
;;; Code:

(setq-default indent-tabs-mode nil)

(provide 'my/disable-tabs)
;;; disable-tabs.el ends here
